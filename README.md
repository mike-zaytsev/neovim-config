# Nix Neovim setup

Minimalist neovim configuration using [Lazy](https://github.com/folke/lazy.nvim) with all
dependencies bundled into a nix flake.

### Installation

```shell
nix profile add github:mike-zaytsev/neovim-config
```
